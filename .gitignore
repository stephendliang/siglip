# Build artifacts — binaries
patch_embed
patch_embed_timing
cutlass-bench
cutlass-bench-max
cutlass-bench-fc1
cutlass-bench-fc2
cutlass-bench-fc1-max
cutlass-bench-fc2-max
cutlass-bench-*
cublas-bench
cublas-bench-fc1
cublas-bench-fc2
fc1-gelu
fc1-gelu-ptx
fc2
calibration
calib-tput
calib-conflict
calib-lat
siglip_vision
siglip_best
siglip_timing
siglip_s0
siglip_s2
siglip_x32
siglip_x64
cutlass-256x256x64

# w3/w3x kernel + bench binaries (dash-named at root; sources use underscores,
# so these never shadow a .cu/.cuh). Root-anchored to stay clear of subdirs.
/fc1-*
/fc2-*
/cublaslt-*
/cublas-bench-*
/calib-*
/tma-bench
/tma-swizzle-probe*
/mma-bench
/stmatrix-bench
/relay-mbar
/warp-scaling

# Build intermediates (nvcc keep files, dlink, etc.)
*.o
*.cubin
*.fatbin
*.fatbin.c
# hand-authored PTX source is a deliverable, not a build artifact
!fc2_w3x.ptx
*.ptx
!fc2_w3x.ptx
*.ii
*.module_id
*.reg.c
*.cudafe1.*
*.stub.c
*_dlink.*
*.gpu

# Libraries
*.so
*.so.*

# SASS dumps (large, regenerable)
sass/

# Test/scratch
tests.txt
k

# Python cache
__pycache__/
*.pyc

# Editor
*.swp
*.swo
*~
/gen/
