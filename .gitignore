# Build artifacts — binaries
patch_embed
patch_embed_timing
cutlass-bench
cutlass-bench-max
cutlass-bench-fc1
cutlass-bench-fc2
cutlass-bench-fc1-max
cutlass-bench-fc2-max
cutlass-bench-*
cublas-bench
cublas-bench-fc1
cublas-bench-fc2
fc1-gelu
fc1-gelu-ptx
fc2
calibration
calib-tput
calib-conflict
calib-lat
siglip_vision
siglip_best
siglip_timing
siglip_s0
siglip_s2
siglip_x32
siglip_x64
cutlass-256x256x64

# w3/w3x kernel + bench binaries (dash-named at root; sources use underscores,
# so these never shadow a .cu/.cuh). Root-anchored to stay clear of subdirs.
/fc1-*
/fc2-*
/cublaslt-*
/cublas-bench-*
/calib-*
/tma-bench
/tma-swizzle-probe*
/mma-bench
/stmatrix-bench
/relay-mbar
/warp-scaling

# Build intermediates (nvcc keep files, dlink, etc.)
*.o
*.cubin
*.fatbin
*.fatbin.c
*.ptx
*.ii
*.module_id
*.reg.c
*.cudafe1.*
*.stub.c
*_dlink.*
*.gpu

# Libraries
*.so
*.so.*

# SASS dumps (large, regenerable) — sass/ holds all disassembly incl. root_dumps/;
# /sass*.txt catches any freshly re-dumped root file before it's moved in.
sass/
/sass*.txt

# Benchmark artifacts — derived, reproducible on the B200, never hand-edited.
# Kept off disk entirely (modal-first); conclusions live in CLAUDE.md / memory/*.md.
/data/

# SASS epilogue templates — regenerable via `tools/template_emitter.py --variant both`.
/templates/

# Test/scratch
tests.txt
k

# Python cache
__pycache__/
*.pyc

# Editor
*.swp
*.swo
*~
/gen/
